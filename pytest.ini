[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -q --tb=short
filterwarnings =
    ignore::DeprecationWarning
markers =
    acceptance: pilot acceptance scenarios A01–A05 + guest
    mutation: mutation probes that must stay refused
